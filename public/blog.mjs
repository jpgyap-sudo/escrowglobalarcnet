const root=document.querySelector('#posts'),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
try{const r=await fetch('/api/blog'),out=await r.json();document.title=`Journal · ${out.settings.siteName}`;root.innerHTML=out.posts.length?out.posts.map(p=>`<a class="post-card" href="/blog/${encodeURIComponent(p.slug)}"><span class="tag">${esc(p.tags?.[0]||'Journal')}</span><h2>${esc(p.title)}</h2><p class="excerpt">${esc(p.excerpt)}</p><p class="byline">${esc(p.author)} · ${new Date(p.publishedAt).toLocaleDateString('en-US',{dateStyle:'long'})}</p></a>`).join(''):'<p class="empty">The journal is being prepared. Check back soon.</p>'}catch(error){root.textContent=error.message}
